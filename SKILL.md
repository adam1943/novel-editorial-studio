---
name: novel-editorial-studio
description: 长篇小说的自动连载与审阅优先编辑助手，覆盖百万字级规划、逐章生成、多维审计、修订、平台合规与连续性管理。Use when Codex needs to plan or run a long-form novel pipeline, draft or continue chapters in batches, diagnose chapter logic and bridges, reduce AI-like tone, audit continuity or platform compliance, or resume a novel project from an on-disk checkpoint.
---

# Novel Editorial Studio

## 总览
把小说当作一个长期项目来管理。Markdown 文件是正史，聊天只是工作台；正文、设定、状态、决策和修订都要落在磁盘上，才能从可靠停点恢复。

默认按百万字级连载设计：卷 -> 卷段 -> 章 -> 场景四层结构，逐批推进，逐章审计，检查点可独立恢复。

## 默认工作方式
- 用户在问“合理吗 / 顺吗 / 有无AI味 / 是否该合并 / 这里跳了没有”，默认进入审阅模式，先找问题，再谈修订。
- 用户明确要“写 / 续写 / 扩写 / 改写”时，进入写作或修订模式。
- 用户明确要“自动推进 / 跑管线 / 少人工干预”时，进入自动连载档，按 `references/autonomous-pipeline.md` 执行。
- 不要把审阅请求直接处理成成品大纲；先判断结构，再给最小修正。

## 何时读取参考
- 新项目或空白创意 -> 读 `references/lifecycle.md` 和 `references/templates.md`
- 继续已有项目 -> 先读 `references/project-state.md`
- 自动连载、批次推进、质量门、停机条件 -> 读 `references/autonomous-pipeline.md`
- 百万字级卷章分配、主线阶梯、中段防塌 -> 读 `references/scale-architecture.md`
- 上下文预算、索引台账、信息边界 -> 读 `references/memory-architecture.md`
- 逐章审计 -> 读 `references/audit-dimensions.md`
- 平台过审与内容红线 -> 读 `references/platform-compliance.md`
- 题材规则与语言铁律 -> 读 `references/genre-rules.md`
- 设计剧情、世界状态或章节包 -> 读 `references/story-control-model.md`
- 审阅大纲、章节逻辑、过渡、关系热度、AI味 -> 读 `references/continuity-and-qa.md`、`references/style-and-humanity.md`，必要时再读 `references/review-mode.md`
- 卷末复盘、规则升级、质量债务 -> 读 `references/evolution.md`
- 想借鉴 `neuro-book` 的工作流思想 -> 读 `references/neuro-book-notes.md`

## 脚本
机检只提供线索，判定始终由审计员完成。
- `scripts/novel_audit.py 正文/ --json`：字数、章号、占位符、跨章重复、套话密度、疲劳词、段落节奏、对白比例
- `scripts/compliance_scan.py 正文/ --json`：内容红线初筛与平台格式基线
- `scripts/book_status.py . --target-words 2000000`：字数进度、章号缺口、支线停滞、伏笔逾期

## 自动连载档
1. 开档前确认题材、主角人设锁、结局方向、数值上限、禁区、目标总字数、单章字数区间、每批章数都已落盘。
2. 单章主链：装配上下文 -> 编章节写作包 -> 写正文 -> 机检 -> 审计 -> 修订 -> 合规 -> 回灌台账 -> 写检查点。
3. 质量门：block 归零，warn 不超阈值，字数达标，且本章至少一处状态变化入账。
4. 同一章最多自动修订三轮；第三轮回到写作包层重编，仍不过则停机报人。
5. 触发停机条件就停：连续同类 block、需要改结局或世界硬规则、合规红线无法在不损坏剧情前提下改写、素材耗尽。
6. 授权范围内直接改：正文、句子修订、连续性修复、台账回灌、章节合并拆分。超范围的记为待批，不静默执行。

## 审阅模式
1. 先判断章节或场景是否应该存在，再判断怎么写。
2. 优先检查桥接：前后章之间的权力、身份、地点、时间、关系、资源是否连得上。
3. 优先检查人物热度：谁为什么靠近，为什么疏远，升温是否有事件支撑。
4. 优先检查剧情必要性：这一章是否真的带来新状态变化；若没有，考虑合并、后移或删除。
5. 优先检查 AI 味：模板句、提示词腔、说明书口吻、重复功能章、空转对话、过密总结。
6. 输出先给问题，再给证据，再给最小修正；不要先写一整版重构稿，除非用户明确要求。

## 写作 / 修订模式
1. 确认题材、主视角、时态、目标篇幅、结局约束和禁区。
2. 建立或更新项目控制包。
3. 章节卡优先只保留三层：章节任务、核心剧情简述、审核意见。
4. 核心剧情写成故事，不写提示词，不写“本章必须”。
5. 写正文时只依据已确认事实，不擅自改世界规则、核心动机或结局。
6. 审校时先修硬逻辑，再修因果、视角、人物声线和行文。
7. 把新增事实与重复修改沉淀回控制包，形成可复用规则。

## 章节卡偏好
- `章节任务`：一句话说明这一章要推进什么。
- `核心剧情简述`：只写现场发生了什么、人物怎么选、代价是什么、下一章从哪里接。
- `审核意见`：只写桥接是否成立、是否有重复、是否有 AI 味、是否需要合并或重排。
- 不写“本章必须围绕……”“状态变化与章末压力……”“本章执行边界……”这类提示词腔，除非用户明确要求保留。

## 项目文件
基础控制包：
- `项目总控.md`
- `创作决策记录.md`
- `连续性台账.md`
- `动态状态事件账本.md`
- `剧情双轨图.md`
- `伏笔台账.md`
- `进度台账.md`

长篇连载追加索引，供上下文装配与审计比对：
- `章节摘要台账.md`
- `支线进度板.md`
- `情感弧线.md`
- `角色矩阵.md`
- `资源账本.md`
- `质量债务.md`

## 平台合规立场
目标是让稿件真正达标从而正常过审，不做规避审核的工作。可以识别并改掉违规内容、调整过界描写、修正格式与字数、降低模板化痕迹。不做关键词变形、拆字谐音替换、零宽字符插入、伪造原创声明或规避平台AI披露要求。用户要求绕审时，说明这条边界，再给出把内容改到达标的路径。发布动作由用户自己完成。

## 结构约束
采用 `neuro-book` 的分层思路：
- Markdown Studio: Markdown 是真相源，编辑器只是视图
- World Engine: 记录变化，不只记录当前状态
- Plot Workbench: 分开“怎么讲”和“为什么发生”
- llmlint: 把风格问题当作可复核的线索，不把命中数当判决

## 写作与审校
- 对话负责施压、试探、遮掩、转折和关系变化，不负责复述双方都知道的事。
- 叙述负责动作落点、空间关系、时间跳转和对白承载不了的信息。
- 修稿时优先修上游问题；只有句子层问题才做局部润色。
- 若修订会动到结局、主视角体系、核心人物或世界规则，先问用户。

## 学习与进化
每批结束后，提取新增事实、重复批注、常见错误和用户偏好，写回控制包。把重复出现的修正升级为站立规则，而不是下一批重新讨论。卷末做一次复盘：主线推进度、支线停滞、伏笔兑现率、字数进度、重复功能章。

## 交付要求
- 审阅请求先给问题、证据、最小修正。
- 改稿请求再给修订后的文件和明确恢复停点。
- 自动连载档每批交付新增章节、修订说明、机检摘要、台账变更、下一批入口、待批候选。
- 若没有明显问题，要直接说清楚，并标注仍然存在的残余风险。
